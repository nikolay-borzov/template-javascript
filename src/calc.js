import { normalizeNumberResult } from './helpers.js'

/**
 * Performs complex calculations.
 *
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */
export function complexCalculations(a, b) {
  return normalizeNumberResult(a + b)
}
